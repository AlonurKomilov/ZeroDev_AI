import React from 'react';

const Topbar = () => {
  return (
    <header className="h-16 bg-card text-card-foreground p-4 border-b flex items-center">
      <h1 className="text-xl font-bold">Topbar</h1>
    </header>
  );
};

export default Topbar;
